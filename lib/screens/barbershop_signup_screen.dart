import 'package:flutter/material.dart';

class BarbershopSignUpScreen extends StatelessWidget {
  const BarbershopSignUpScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          'Barbershop Sign Up Screen',
        ),
      ),
    );
  }
}